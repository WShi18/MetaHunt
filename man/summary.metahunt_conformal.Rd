% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conformal.R
\name{summary.metahunt_conformal}
\alias{summary.metahunt_conformal}
\title{Summarise a conformal prediction-interval object}
\usage{
\method{summary}{metahunt_conformal}(object, ...)
}
\arguments{
\item{object}{A \code{"metahunt_conformal"} object from \code{\link[=split_conformal]{split_conformal()}},
\code{\link[=cross_conformal]{cross_conformal()}}, or \code{\link[=conformal_from_fit]{conformal_from_fit()}}.}

\item{...}{Unused; present for S3 generic consistency.}
}
\value{
A list of class \code{"summary.metahunt_conformal"}. In pointwise mode
(no wrapper) the list contains \code{n_targets}, \code{G_grid}, \code{n_cal}, \code{alpha},
\code{method}, \code{mean_interval_width}, \code{frac_finite_quantile},
\code{quantile_summary}, and \code{wrapper}. In scalar mode (wrapper supplied)
the list contains \code{n_targets}, \code{n_cal}, \code{alpha}, \code{method},
\code{mean_interval_width}, \code{quantile}, \code{quantile_finite}, and \code{wrapper}.
}
\description{
Produces a small list of descriptive statistics about a
\code{"metahunt_conformal"} object: interval widths, quantile summaries, and
calibration diagnostics. Returns an object of class
\code{"summary.metahunt_conformal"} with a matching \code{print} method.
}
\examples{
set.seed(1)
G <- 25; m <- 60
x <- seq(0, 1, length.out = G)
basis <- rbind(sin(pi * x), cos(pi * x), x)
W <- data.frame(w1 = rnorm(m))
eta <- cbind(0.6 * W$w1, -0.3 * W$w1, rep(0, m))
pi_true <- exp(eta) / rowSums(exp(eta))
F_hat <- pi_true \%*\% basis + matrix(rnorm(m * G, sd = 0.05), m, G)
W_new <- data.frame(w1 = c(0, 1))
res <- split_conformal(F_hat, W, W_new, K = 3,
                       dfspa_args = list(denoise = FALSE), seed = 1)
summary(res)

}
